[package]
name = "foxy-ircd"
version = "0.1.0"
authors = ["Solra Bizna <solra@bizna.name>"]
edition = "2018"

[dependencies]
arrayref = "0.3"
tokio = {version = "0.2", features=["rt-threaded", "io-std", "io-util", "tcp", "macros", "dns", "fs", "sync"]}
serde_json = "1.0"
